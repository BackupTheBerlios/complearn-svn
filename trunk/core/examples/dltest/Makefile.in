CFLAGS=-I@top_srcdir@/src

all: libart.so.1.0.1

%.o: %.c
	gcc -fPIC -g -c -Wall $< $(CFLAGS)

libart.so.1.0.1: libart.o
	gcc -shared -Wl,-soname,libart.so.1 -o libart.so.1.0.1 libart.o @top_srcdir@/src/.libs/libcomplearn.so -lc -lm

install:
	echo "(no installation)"

distclean: clean


clean:
	rm libart.o libart.so.*

distdir:
	echo "(no distdir)"
