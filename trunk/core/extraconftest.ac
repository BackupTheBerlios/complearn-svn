
AC_INIT(libzs,0.4.9,cilibrar@cilibrar.com)
AC_CONFIG_SRCDIR(src/t.c)
AM_INIT_AUTOMAKE
AC_CANONICAL_HOST
AM_PROG_CC_C_O
AM_PATH_CHECK
AM_MAINTAINER_MODE

AM_PATH_COMPLEARN(0.9.9, [
  CFLAGS="$CFLAGS $COMPLEARN_CFLAGS"
  LIBS="$LIBS $COMPLEARN_LIBS"
  ], [
    AC_MSG_ERROR(must have complearn >= 0.9.9 installed)
  ])
PRIVATELIBS="$LIBS"

AC_SUBST(PRIVATELIBS)

AC_OUTPUT([ src/Makefile ])
